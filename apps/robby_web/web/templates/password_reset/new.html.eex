<h1>Enter Email</h1>

<%= form_for @conn, password_reset_path(@conn, :create), [as: :password_reset], fn f -> %>
  <div class="form-group">
    <%= email_input f, :email, placeholder: "Email", class: "form-control" %>
  </div>
  <%= submit "Reset", class: "btn btn-primary" %>
<% end %>
